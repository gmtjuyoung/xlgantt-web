# 구현 현황 (2026-03-27 기준)

## 파일 구조

```
xlgantt-web/
├── src/
│   ├── App.tsx                          # 라우팅, 메인 앱
│   ├── main.tsx                         # 엔트리 포인트
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.tsx             # 전체 레이아웃 쉘
│   │   │   └── Header.tsx              # 상단 네비게이션 (탭 메뉴)
│   │   ├── gantt/
│   │   │   ├── GanttView.tsx           # 스케줄 탭 메인 (테이블+차트 분할)
│   │   │   ├── GanttToolbar.tsx        # 작업 추가/삭제/들여쓰기/이동 툴바
│   │   │   ├── TaskTable.tsx           # WBS 테이블 (좌측)
│   │   │   ├── TaskRow.tsx             # 작업 행 컴포넌트
│   │   │   ├── TaskCell.tsx            # 인라인 편집 셀
│   │   │   ├── TaskEditDialog.tsx      # 작업 상세편집 다이얼로그
│   │   │   ├── GanttChart.tsx          # 간트차트 (우측)
│   │   │   ├── GanttTimescale.tsx      # 타임스케일 헤더
│   │   │   └── GanttBar.tsx            # 간트바 렌더링 (계획/실적/마일스톤)
│   │   ├── progress/
│   │   │   └── ProgressDashboard.tsx   # 진척현황 탭
│   │   ├── analysis/
│   │   │   └── AnalysisReport.tsx      # 분석리포트 탭
│   │   ├── workload/
│   │   │   └── WorkloadView.tsx        # 작업량 탭
│   │   ├── calendar/
│   │   │   └── CalendarManager.tsx     # 달력 탭
│   │   ├── settings/
│   │   │   ├── ResourceManager.tsx     # 담당자 탭
│   │   │   └── ProjectSettings.tsx     # 설정 탭
│   │   └── ui/                         # shadcn/ui 공통 컴포넌트 (13개)
│   ├── stores/
│   │   ├── task-store.ts               # Zustand - 작업 데이터 상태
│   │   ├── resource-store.ts           # Zustand - 리소스/담당자 상태
│   │   ├── project-store.ts            # Zustand - 프로젝트 설정 상태
│   │   └── ui-store.ts                 # Zustand - UI 상태
│   └── lib/
│       ├── types.ts                    # 전체 타입 정의
│       ├── resource-types.ts           # 리소스 관련 타입
│       ├── sample-data.ts              # 샘플 데이터 (ABC 프로젝트)
│       ├── gantt-math.ts               # 간트 좌표 계산
│       ├── calendar-calc.ts            # 근무일 계산
│       ├── dependency-routing.ts       # 의존관계 화살표 경로 계산
│       ├── progress-calc.ts            # 진척률 계산
│       ├── wbs.ts                      # WBS 코드 생성/관리
│       ├── excel-import.ts             # 엑셀 파일 임포트
│       ├── supabase.ts                 # Supabase 클라이언트
│       └── utils.ts                    # 유틸리티
└── docs/                               # 이 문서들
```

## 페이지별 구현 상태

### 1. 스케줄 (메인) ✅ 기본 구현됨
- [x] WBS 계층 테이블 (좌측)
- [x] 간트차트 (우측) - 주간 단위
- [x] 간트바 렌더링 (계획바: 파란색, 실적바: 회색)
- [x] 그룹 작업 자동 요약바
- [x] 마일스톤 다이아몬드 표시
- [x] 의존관계 화살표
- [x] 오늘선 표시
- [x] 툴바 (추가/삭제/들여쓰기/내어쓰기/위로/아래로 이동)
- [x] 인라인 편집 (작업명, 날짜, 기간, 작업량, 진척률)
- [x] 작업 상세편집 다이얼로그
- [x] 담당자 컬럼 표시

### 2. 진척현황 ✅ 기본 구현됨
- [x] 핵심 수치 7개 (총 작업량, 계획/실적 작업량, 계획/실적 진척률, 차이)
- [ ] 기준일자 변경 기능 (Status Date)

### 3. 분석리포트 ✅ 기본 구현됨
- [x] 월별 진척 테이블
- [x] S-커브 라인 차트 (Recharts)
- [ ] 기간 선택 기능

### 4. 작업량 ✅ 기본 구현됨
- [x] 기본 작업량 표시
- [ ] 리소스×기간 크로스탭
- [ ] 히스토그램 차트
- [ ] 주간/월간 전환

### 5. 달력 ✅ 기본 구현됨
- [x] 공휴일 목록 테이블
- [ ] 월간 캘린더 뷰
- [ ] 3개 달력 관리 (STD/UD1/UD2)

### 6. 담당자 ✅ 기본 구현됨
- [x] 담당자 등록 (이름, 회사, 역할, 이메일)
- [x] 회사별 색상
- [ ] 담당자 수정 기능
- [ ] 회사 색상 수정

### 7. 설정 ✅ 기본 구현됨
- [x] 프로젝트명, 기간 설정
- [x] 근무일 설정
- [ ] 색상 테마 선택
- [ ] 언어 전환 (한/영)

---

## 디자인/UI 상태
- 전체 리디자인 완료 (Impeccable 스킬 적용)
- 1920x1080 최적화 폰트 크기 2배 확대 적용
- 다크/라이트 없음 (라이트 모드 전용)
